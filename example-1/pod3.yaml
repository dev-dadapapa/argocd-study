apiVersion: v1
kind: Pod
metadata:
  name: argocd-world3
  labels:
    name: argocd-world3
spec:
  containers:
  - name: argocd-world3
    image: nginx
    resources:
      limits:
        memory: "64Mi"
        cpu: "0.3"
    ports:
      - containerPort: 80
