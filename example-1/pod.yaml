apiVersion: v1
kind: Pod
metadata:
  name: argocd-world
  labels:
    name: argocd-world
spec:
  containers:
  - name: argocd-world
    image: nginx
    resources:
      limits:
        memory: "64Mi"
        cpu: "0.3"
    ports:
      - containerPort: 80
